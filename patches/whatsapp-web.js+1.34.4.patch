diff --git a/node_modules/whatsapp-web.js/src/Client.js b/node_modules/whatsapp-web.js/src/Client.js
index 6a67629..40aeee4 100644
--- a/node_modules/whatsapp-web.js/src/Client.js
+++ b/node_modules/whatsapp-web.js/src/Client.js
@@ -212,6 +212,7 @@ class Client extends EventEmitter {
         });
 
         await exposeFunctionIfAbsent(this.pupPage, 'onAppStateHasSyncedEvent', async () => {
+          try {
             const authEventPayload = await this.authStrategy.getAuthEventPayload();
             /**
                  * Emitted when authentication is successful
@@ -241,13 +242,14 @@ class Client extends EventEmitter {
                 }
                 let start = Date.now();
                 let res = false;
-                while(start > (Date.now() - 30000)){
+                while(start > (Date.now() - 90000)){ // patched: 30s was too short for large sessions
                     // Check window.Store Injection
                     res = await this.pupPage.evaluate('window.Store != undefined');
                     if(res){break;}
                     await new Promise(r => setTimeout(r, 200));
                 }
                 if(!res){
+                    console.error('whatsapp-web.js: window.Store injection timed out after 90s - WhatsApp Web took too long to sync');
                     throw 'ready timeout';
                 }
             
@@ -272,6 +274,10 @@ class Client extends EventEmitter {
                  */
             this.emit(Events.READY);
             this.authStrategy.afterAuthReady();
+          } catch (e) {
+            console.error('whatsapp-web.js: onAppStateHasSyncedEvent crashed (client stuck forever otherwise) - ' + (e && e.stack ? e.stack : e));
+            setTimeout(() => process.exit(1), 500);
+          }
         });
         let lastPercent = null;
         await exposeFunctionIfAbsent(this.pupPage, 'onOfflineProgressUpdateEvent', async (percent) => {
@@ -756,7 +762,7 @@ class Client extends EventEmitter {
             window.Store.Msg.on('change:body change:caption', (msg, newBody, prevBody) => { window.onEditMessageEvent(window.WWebJS.getMessageModel(msg), newBody, prevBody); });
             window.Store.AppState.on('change:state', (_AppState, state) => { window.onAppStateChangedEvent(state); });
             window.Store.Conn.on('change:battery', (state) => { window.onBatteryStateChangedEvent(state); });
-            window.Store.Call.on('add', (call) => { window.onIncomingCall(call); });
+            if (window.Store.Call) { window.Store.Call.on('add', (call) => { window.onIncomingCall(call); }); } else { console.error('whatsapp-web.js: window.Store.Call missing - skipping incoming-call listener (WhatsApp removed/renamed this module)'); }
             window.Store.Chat.on('remove', async (chat) => { window.onRemoveChatEvent(await window.WWebJS.getChatModel(chat)); });
             window.Store.Chat.on('change:archive', async (chat, currState, prevState) => { window.onArchiveChatEvent(await window.WWebJS.getChatModel(chat), currState, prevState); });
             window.Store.Msg.on('add', (msg) => { 
diff --git a/node_modules/whatsapp-web.js/src/util/Injected/Store.js b/node_modules/whatsapp-web.js/src/util/Injected/Store.js
index 3f2c8c2..b92b15f 100644
--- a/node_modules/whatsapp-web.js/src/util/Injected/Store.js
+++ b/node_modules/whatsapp-web.js/src/util/Injected/Store.js
@@ -44,6 +44,13 @@ exports.ExposeStore = () => {
     };
 
     window.Store = Object.assign({}, window.require('WAWebCollections'));
+    // patched: WhatsApp renamed several collection keys inside WAWebCollections
+    // (GroupMetadata -> WAWebGroupMetadataCollection etc.) which broke
+    // getChatModel/attachEventListeners. Alias them back.
+    window.Store.GroupMetadata = window.Store.GroupMetadata || window.Store.WAWebGroupMetadataCollection;
+    window.Store.Call = window.Store.Call || window.Store.WAWebCallCollection;
+    window.Store.NewsletterCollection = window.Store.NewsletterCollection || window.Store.WAWebNewsletterCollection;
+    window.Store.NewsletterMetadataCollection = window.Store.NewsletterMetadataCollection || window.Store.WAWebNewsletterMetadataCollection;
     window.Store.AppState = window.require('WAWebSocketModel').Socket;
     window.Store.BlockContact = window.require('WAWebBlockContactAction');
     window.Store.Conn = window.require('WAWebConnModel').Conn;
diff --git a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
index af2a39a..e11e02c 100644
--- a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
+++ b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
@@ -655,10 +655,17 @@ exports.LoadUtils = () => {
 
         model.lastMessage = null;
         if (model.msgs && model.msgs.length) {
-            const lastMessage = chat.lastReceivedKey
-                ? window.Store.Msg.get(chat.lastReceivedKey._serialized) || (await window.Store.Msg.getMessagesById([chat.lastReceivedKey._serialized]))?.messages?.[0]
-                : null;
-            lastMessage && (model.lastMessage = window.WWebJS.getMessageModel(lastMessage));
+            // patched: WhatsApp changed the lastReceivedKey object shape - it no
+            // longer carries _serialized. Passing undefined into Msg.getMessagesById
+            // hits IndexedDB with no key and throws DataError, which crashed every
+            // getChat/getChatModel call. lastMessage is non-critical, so guard it.
+            try {
+                const lastKeySer = chat.lastReceivedKey && chat.lastReceivedKey._serialized;
+                const lastMessage = lastKeySer
+                    ? window.Store.Msg.get(lastKeySer) || (await window.Store.Msg.getMessagesById([lastKeySer]))?.messages?.[0]
+                    : null;
+                lastMessage && (model.lastMessage = window.WWebJS.getMessageModel(lastMessage));
+            } catch (e) { /* non-critical - leave lastMessage null */ }
         }
 
         delete model.msgs;
